[pytest]
; Adds app/ to Python's import path, so files in test/ can still do
; "from sorting_hat_confidence import ..." without any changes to their
; import statements, even though the code being tested now lives in a
; separate folder.
pythonpath = app
testpaths = test
